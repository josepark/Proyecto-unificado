# SUIIN-SGSI-RIESGOS — Frontend

Ver el `README.md` en la raíz del proyecto para la documentación completa
(arquitectura, instalación de backend + frontend, y referencia de la API).

Inicio rápido:

```bash
npm install
cp .env.example .env    # opcional, ya trae el valor por defecto correcto
npm run dev              # http://localhost:5173
```
